# Leases

Leases prevent cross-domain collisions.

- Each lease is a JSON file created by `./scripts/oxygen claim ...`.
- Leases are short-lived and expire automatically by timestamp.

This directory is stateful and should be treated as sensitive.
