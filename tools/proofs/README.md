# Proof Tool

Creates deterministic discovery attestations.

Entry:
- `./scripts/proof`

Commands:

```bash
./scripts/proof create --title "..." --claim "..." --evidence path --evidence path
./scripts/proof verify --attestation artifacts/proofs/...json
```
