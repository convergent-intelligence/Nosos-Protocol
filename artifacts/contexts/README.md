# Context Capsules

Context capsules are "just enough" task context.

They are generated by `./scripts/context` and linked from the work item entry in:
- `tools/plan-registry/plans.yaml`

See:
- `docs/context-protocol.md`
